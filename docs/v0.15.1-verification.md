# v0.15.1 verification

23 September 2026, approved option A with retained Admin Panel.

- 144 automated tests passed. One production-copy rehearsal test is skipped in
  the default suite and passed separately against a fresh production backup.
- All existing data across 32 tables remained identical after the rehearsal.
- Lint, TypeScript, regression checks and production build passed.
- Full dependency audit: zero vulnerabilities; dependency versions unchanged.
- Production-build Chromium checks passed at 320, 390, 768 and 1536px without
  horizontal overflow or browser errors; mobile navigation remains usable.
- Search finds older elections beyond the previous ten-item limit. All six status
  filters and empty/reset states passed.
- Setup continuation and proof/takeover affordances are retained; existing route
  tests continue to enforce draft ownership and audited takeover.
- Browser tests exercised Owner, Admin, Observer, Returning Officer and unassigned
  users, including Owner-only archive data absent from other users' HTML payloads.
- Finalisation links go to the management page rather than mutating election state.
- A new cold-start regression confirms the legacy migration cannot recreate a
  revoked election assignment or grant unrelated access on startup.

Browser testing used isolated fixtures and sent no emails/SMS. Release screenshots
use demo data. The change does not modify ballot data or existing role assignments.
