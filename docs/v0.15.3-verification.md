# v0.15.3 verification

- 159 automated tests passed; production-copy rehearsal also passed separately.
- New recovery tests cover Owner-only and target restrictions, CSRF, token
  hashing, expiry, replacement, rate limits, delivery failure, password validation,
  replay and concurrent submissions, account changes before and during hashing,
  session invalidation, ordinary login, and unchanged roles/election teams.
- All original fields and rows across 32 existing production tables remained
  unchanged after migration rehearsal; the reset table is additive.
- npm ci, lint, regression checks, type-check, production build and dependency
  audit passed. No reported vulnerabilities and no dependency version changes.
- Chromium acceptance covers Owner confirmation/cancellation, send failure and
  success, URL-fragment removal, no-store/no-referrer headers, password-manager
  autocomplete, mismatch/expired/missing-token states and successful completion
  returning to normal sign-in. 320/390/768px reset forms have no overflow.
- Browser API delivery was mocked; server route/domain tests used mocked email
  transport and isolated accounts. No real emails or credentials were sent.
- Actual iPhone password-manager behaviour is not verified by Chromium testing.
