# v0.15.4 verification

- 168 automated tests passed. Recovery coverage includes 21 tests for assisted
  and self-service recovery: every role including Owner, CSRF/input validation,
  generic responses before delivery, independent rate limits, concurrent requests,
  expiry/replay, failed delivery, older-link preservation, account changes,
  session revocation and unchanged roles/election assignments.
- Populated legacy reset-table migration preserves all fields, indexes and
  AUTOINCREMENT high-water mark and is idempotent.
- Fresh production-copy migration rehearsal passed. All original fields and rows
  across 33 tables were unchanged; integrity and foreign-key checks passed.
- npm ci, lint, type-check, regression checks, production build and dependency
  audit passed. Zero reported vulnerabilities; dependency versions unchanged.
- Chromium acceptance passed: login navigation, private page headers, form errors,
  generic success, retry, expired-link recovery and 320/390/768/1440px layouts.
  An actual request for a proven-absent local fixture address verified that Next
  runs the after-response callback. Known-account delivery was mocked.
- No real account recovery email was sent by tests or deployment. Actual iPhone
  password-manager behaviour has not been verified by these Chromium checks.
